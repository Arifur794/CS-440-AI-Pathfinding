package heuristics;

public class DistanceHeuristic {
	private int bx, by, sx, sy;
	
	public DistanceHeuristic(Grid g) {
		super(g);
		bx = g.endCell[1];
		by = g.endCell[0];
		sx = g.startCell[1];
		sy = g.endCell[0];
}
