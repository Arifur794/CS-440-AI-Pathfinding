package searches;

import gui.model.Grid;

public class AStar extends SearchAlgo {
	public AStar(Node start, Node goal, Grid grid, Heuristic func) {
		super(start, goal, grid);	
		this.func = func;
	}
}
